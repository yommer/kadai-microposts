<% if micropost.liking?(current_user) %>
    <%= form_for(micropost.likes.find_by(user_id: current_user.id), html: { method: :delete }) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'Unlike', class: 'btn btn-info btn-xs' %>
    <% end %>
  <% else %>
    <%= form_for(micropost.likes.build) do |f| %>
      <%= hidden_field_tag :micropost_id, micropost.id %>
      <%= f.submit 'Like♡', class: 'btn btn-warning btn-xs' %>
    <% end %>
  <% end %>